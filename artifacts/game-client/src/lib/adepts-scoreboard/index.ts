/**
 * Shared scoreboard for `/adepts-game/`, `/adepts-game/2/`, `/adepts-game/3/` (legacy `/adepts-game-*` URLs redirect).
 * Standalone `artifacts/adepts-game*` apps may still duplicate; sync from here if needed.
 */
export { Scoreboard } from "./Scoreboard";
