/**
 * Shared quiz grid for `/adepts-game`, `/adepts-game-2`, `/adepts-game-3` (`board` prop).
 * Standalone `artifacts/adepts-game*` may duplicate; sync from here if needed.
 */
export { QuizBoard } from "./QuizBoard";
